// Mirrors AUTH_PROXY_PREFIX in apps/gateway/src/auth-proxy.ts. Kept here to
// avoid the web bundle importing from a server app; if the gateway prefix
// changes both constants must be updated together.
export const AUTH_PROXY_PATH = "/api/auth";
